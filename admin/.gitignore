# Local/seeded SQLite databases (demo data, not source)
*.db

# Frontend dependencies and build output
node_modules/
dist/

# TypeScript incremental build cache
*.tsbuildinfo

# Local env files (commit only .env.example)
.env
